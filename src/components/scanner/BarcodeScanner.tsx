import { View, Text } from 'react-native';

export function BarcodeScanner() {
  return (
    <View className="flex-1 items-center justify-center bg-cream">
      <Text className="text-sage-700">BarcodeScanner — Phase 2</Text>
    </View>
  );
}
